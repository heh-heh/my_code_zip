#include <stdio.h>

int main(){
    int n=0, m=0;
    char pos[500][4] = {" "};
    char name[500][120] = {" "};
    int abm[500][3] = {0};
    
    int max_ov=0, max_money=0;
    int sum1=0, sum2 =0;

    scanf("%d %d", &n, &m);
    for(int i=0; i<m; i++) scanf("%s %d %d %d %s", pos[i], &abm[i][0],&abm[i][1],&abm[i][2],na[i]);



    for(int i=0; i<)
}