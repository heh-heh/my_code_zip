#include <stdio.h>
#include <stdlib.h>
#incldue <time.h>

int main()
{
    int max =0;
    pritnf("최대 몇명 인지 입력 하세요 : ")
    scanf("%d", &max);
    int usr[max+1]={0};
    int i=1, num =0;
    while(1)
    {
        srand(time(NULL));
        num=
        printf("%d 번 째 친구의 자리는 %d 입니다" i,num);
    }
    return 0;
}
